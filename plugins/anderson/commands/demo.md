---
description: "Preview the anderson pipeline UX (every banner + both gates) with no agents and no tokens."
allowed-tools: Bash(bash:*)
---
A full dry-run of the loop — no agents dispatched, no tokens spent, instant:
!`DEMO_DELAY=0 bash "${CLAUDE_PLUGIN_ROOT}/bin/demo.sh"`
